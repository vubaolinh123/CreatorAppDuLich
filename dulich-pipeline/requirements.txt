# Core deps — đủ để chạy app FREE 100% (render video nv1-5 + tin tức, giọng gTTS).
# Cài: pip install -r requirements.txt  &&  playwright install chromium
# Ngoài ra cần FFmpeg trên PATH (xem README) — không cài qua pip được.

gtts>=2.5.0            # giọng đọc free (Google), mặc định
edge-tts>=6.1.3       # giọng đọc free (Microsoft), fallback
Pillow>=10.0.0        # vẽ overlay/caption (PIL)
playwright>=1.49.0    # render overlay HTML nv2/nv3 + quét tin tức (Chromium)
requests>=2.31.0      # gọi API (OpenRouter/APIFY/Vbee… — đều optional)
python-dotenv>=1.0.1  # đọc .env
pydantic>=2.10.0      # schema/config
pymongo>=4.8.0        # DB (tự fallback file mock nếu không có Mongo)
google-api-python-client
google-auth
